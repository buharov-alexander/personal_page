import React from 'react';
import {
  Container,
} from 'react-bootstrap';

const Contacts = () => (
  <section>
    <Container className="b-section">
    </Container>
  </section>
);

export default Contacts;
